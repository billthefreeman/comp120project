<div class="chatboxhead" style="position:relative; z-index:98;">
  <div class="chatboxtitle">
    <i class="fa fa-comments"></i>

    <h1><%= @reciever.user_name %> </h1>
  </div>
  <div class="chatboxoptions">
    <%= link_to "<i class='glyphicon glyphicon-minus'></i> ".html_safe, "#", class: "toggleChatBox", "data-cid" => @conversation.id %>
    &nbsp;&nbsp;
    <%= link_to "<i class='glyphicon glyphicon-remove'></i> ".html_safe, "#", class: "closeChat", "data-cid" => @conversation.id %>
  </div>
</div>
<div class="chatboxcontent">
  <% if @messages.any? %>
      <%= render @messages %>
  <% end %>
</div>
<div class="chatboxinput">
  <%= form_for([@conversation, @message], :remote => true, :html => { id: "conversation_form_#{@conversation.id}"}) do |f| %>
      <%= f.text_area :body, class: "chatboxtextarea", "data-cid" => @conversation.id %>
      <div class="chat-form-btns">    
         <%= f.submit "send", class: "btn btn-primary btn-sm btn-submit"%>

    </div>
  <% end %>
</div>

<%= subscribe_to conversation_path(@conversation) %>